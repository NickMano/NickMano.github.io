import React from 'react';
import '../assets/styles/components/Portfoil.scss';

const Portfoil = ({children}) => (
    <section id="portfoil" className="portfoil">
        {children}
    </section>
) 

export default Portfoil